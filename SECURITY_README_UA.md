# 🚨 КРИТИЧНО: Інструкції з безпеки

## ⚠️ Що сталося?
Telegram Bot Token та Gmail credentials були випадково закомічені в Git і запушені на GitHub.

## 🔥 ЩО РОБИТИ ЗАРАЗ (НЕГАЙНО!)

### 1️⃣ Скасуйте старий Telegram Bot Token (НАЙВАЖЛИВІШЕ!)

**Це потрібно зробити ПРЯМО ЗАРАЗ, перш ніж щось інше!**

1. Відкрийте Telegram
2. Знайдіть бота **@BotFather**
3. Відправте команду: `/mybots`
4. Виберіть свого бота зі списку
5. Натисніть **"API Token"**
6. Натисніть **"Revoke current token"**
7. Підтвердіть скасування
8. Натисніть **"Generate new token"**
9. **ЗБЕРЕЖІТЬ новий токен** (він буде потрібен далі)

### 2️⃣ Очистіть історію Git

```bash
cd /Users/blatik/Documents/rust_apps/lead_gen_0ae77895
./fix_security.sh
```

Цей скрипт:
- Створить резервну копію репозиторію
- Встановить `git-filter-repo` (якщо потрібно)
- Видалить всі сліди секретів з історії Git

### 3️⃣ Force Push на GitHub

**УВАГА:** Це перепише історію на GitHub!

```bash
git push origin --force --all
git push origin --force --tags
```

### 4️⃣ Оновіть секрети в Cloudflare

```bash
wrangler secret put TELEGRAM_BOT_TOKEN
```

Коли запитає, вставте **НОВИЙ** токен, який ви отримали від @BotFather.

### 5️⃣ Оновіть Gmail App Password (Рекомендовано)

1. Перейдіть: https://myaccount.google.com/security
2. Знайдіть **"2-Step Verification"** → **"App passwords"**
3. Видаліть старий app password
4. Створіть новий app password
5. Оновіть локальний файл `Secrets.toml`:
   ```toml
   GMAIL_USER = "ihortovstoles@gmail.com"
   GMAIL_PASSWORD = "ваш-новий-app-password"
   ```

## 📋 Що було зроблено автоматично

✅ Видалено hardcoded токени з усіх файлів  
✅ Оновлено `.gitignore` для запобігання майбутніх витоків  
✅ Створено `Secrets.toml.example` як шаблон  
✅ Видалено `Secrets.toml` з Git tracking  
✅ Створено скрипт `fix_security.sh` для очищення історії  
✅ Створено звіт про інцидент `SECURITY_INCIDENT.md`  

## 🛡️ Як запобігти цьому в майбутньому

1. **Ніколи** не комітьте файли з паролями/токенами
2. Завжди перевіряйте `git status` перед `git add`
3. Використовуйте `git diff --cached` перед `git commit`
4. Переконайтеся, що `.gitignore` містить всі чутливі файли
5. Використовуйте змінні оточення або secret management tools

## 📞 Потрібна допомога?

Перегляньте детальний звіт: `SECURITY_INCIDENT.md`

## ✅ Чеклист

- [ ] Скасовано старий Telegram bot token через @BotFather
- [ ] Створено новий Telegram bot token
- [ ] Запущено `./fix_security.sh`
- [ ] Виконано `git push origin --force --all`
- [ ] Оновлено Cloudflare secret: `wrangler secret put TELEGRAM_BOT_TOKEN`
- [ ] Оновлено Gmail app password (опціонально, але рекомендовано)
- [ ] Оновлено локальний `Secrets.toml` з новими credentials

---

**Час має значення!** Чим швидше ви скасуєте старі токени, тим менший ризик зловживання.
